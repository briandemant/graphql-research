type Listing {
	id: SimpleID!
	title: NonEmptyString!
	slug: NonEmptyString!
	owner: User!
}

extend type Query {
	listing(id: SimpleID!): Listing
#	 	listings: [Listing!]!
	# 	frontPageListings(After: Int = 0): [Listing!]!
}
