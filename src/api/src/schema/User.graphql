type User {
	id: SimpleID!
	name: NonEmptyString!
	# description: String!
	# createdAt: ValidDate!
	# listings: [Listing!]!
}

extend type Query {
	# # 	users: [User!]!
	user(id: SimpleID!): User
	# 	user(id: ID!): User
}
