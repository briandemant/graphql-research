type User {
	id: SimpleID!
	name: NonEmptyString!
	listings: [Listing!]!
	# description: String!
	# createdAt: ValidDate!
}

extend type Query {
	# # 	users: [User!]!
	user(id: SimpleID!): User
	# 	user(id: ID!): User
}
