// import db from './../data/data'

// export default {
// 	Query: {
// 		// fieldName(obj, args, context, info) { result }
// 		user: async (_, { id }) => {
// 			return db.users.find(user => user.id === id)
// 		},
// 		users: () => db.users,
// 	},

// 	User: {
// 		listings(user, args, context, info) {
// 			return db.listings.filter(listing => listing.userId === user.id)
// 		},
// 	},
// }
